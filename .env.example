# Driver Scheduling Application - Environment Configuration

# MongoDB Configuration (Required)
# For local development, install MongoDB and use:
MONGODB_URI=mongodb://localhost:27017/driver-scheduling

# For MongoDB Atlas (cloud), use your connection string:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/driver-scheduling?retryWrites=true&w=majority

# Server Configuration
PORT=5000